generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model RFIDTag {
  id        String   @id @default(cuid())
  tagId     String   @unique
  timestamp DateTime @default(now())
}